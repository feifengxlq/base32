// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("base32", "for base32 support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("base32", "enable base32 support", "no");

if (PHP_BASE32 != "no") {
	EXTENSION("base32", "base32.c");
}

